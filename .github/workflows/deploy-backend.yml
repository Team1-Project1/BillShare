name: Deploy Backend to AWS ECR

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy-backend:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Lấy code
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Cài JDK 17
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      # 3️⃣ Build file JAR
      - name: Build backend with Maven
        run: mvn clean package -DskipTests
        working-directory: ./backend

      # ⚙️ 4️⃣ Cấu hình thông tin AWS credentials (rất quan trọng)
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      # 5️⃣ Login vào ECR
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # 6️⃣ Build & Push Docker image
      - name: Build and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: ${{ secrets.AWS_ECR_REPO }}
          IMAGE_TAG: latest
        run: |
          echo "Building image..."
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG ./backend
          echo "Pushing image to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG


      # 6️⃣ Deploy lên App Runner
#      - name: Deploy to AWS App Runner
#        uses: awslabs/amazon-app-runner-deploy@v1
#        with:
#          service: ${{ secrets.APP_RUNNER_SERVICE_NAME }}
#          image: ${{ steps.login-ecr.outputs.registry }}/${{ secrets.AWS_ECR_REPO }}:latest
#          access-role-arn: ${{ secrets.APP_RUNNER_ROLE_ARN }}
#          region: ${{ secrets.AWS_REGION }}
